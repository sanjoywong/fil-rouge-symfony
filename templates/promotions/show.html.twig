{% extends 'base.html.twig' %}

{% block title %}Promotions{% endblock %}

{% block body %}
    <h1>Promotions</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ promotion.id }}</td>
            </tr>
            <tr>
                <th>Nom_promotion</th>
                <td>{{ promotion.nomPromotion }}</td>
            </tr>
            <tr>
                <th>Annee</th>
                <td>{{ promotion.annee }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_promotions_index') }}">back to list</a>

    <a href="{{ path('app_promotions_edit', {'id': promotion.id}) }}">edit</a>

    {{ include('promotions/_delete_form.html.twig') }}
{% endblock %}
